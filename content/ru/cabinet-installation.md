[Все инструкции](README.md) · [Русский](../ru/cabinet-installation.md) / [English](../en/cabinet-installation.md)

# Установка кабинета и Mini App

Кабинет (`sn-cabinet`) — отдельная служба на том же или другом сервере. Он обслуживает витрину, личный кабинет и `/app/` для Mini App через ограниченный API панели. Доступ к PostgreSQL ему не нужен.

## Порядок установки

1. Откройте **Кабинет и Mini App**. Заполните бренд, тексты, SEO, тарифные описания и инструкции RU/EN. Опубликуйте собственное содержимое.
2. Создайте установку: имя, HTTPS-домен, IP и вариант размещения. Домен кабинета отличается от панели и подписки даже на одном сервере.
3. Направьте DNS A/AAAA на выбранный сервер, откройте TCP 80/443, сохраните доступ SSH.
4. Скопируйте команду из карточки установки и выполните от root на сервере назначения. Ключ относится к этой установке и остаётся в её окружении.
5. Дождитесь проверок бинарника, SHA256, локального `/ready` и публичного HTTPS. Убедитесь, что установка появилась на связи в админке.
6. Выберите эту установку для Mini App в настройках бота. Без кабинета Mini App недоступно.
7. Проверьте витрину до входа, регистрацию, сохранение и повторный просмотр кода, вход с другого браузера и привязку Telegram. Затем проверьте тарифы, докупки, платежи и поддержку.

## На одном сервере

Установщик добавляет отдельный сайт Caddy через `/etc/caddy/customer-sites/cabinet.caddy`. Существующая панель продолжает работать. Не используйте её домен для кабинета. При занятых портах другим proxy не отключайте его вслепую: выберите режим внешнего proxy.

## На отдельном сервере

Нужны Debian/Ubuntu, systemd, amd64/arm64 и исходящий HTTPS к панели. База и её пароль не копируются. Служба слушает `127.0.0.1:8090`; reverse proxy публикует только HTTPS-домен.

## Окружение и диагностика

Секреты: `/etc/sn-cabinet/env`, права 600. Служба: `sn-cabinet`. Установщик создаёт команду обновления:

```bash
/usr/local/sbin/update-cabinet
systemctl status sn-cabinet --no-pager
journalctl -u sn-cabinet -n 80 --no-pager
curl -fsS http://127.0.0.1:8090/ready
```

После обновления проверьте также публичный `/ready` вашего домена. После отзыва установки или её ключа обновите окружение по новой команде из панели. Отзыв ключа не удаляет клиентские покупки.

При существующем nginx или контейнерном proxy используйте `CABINET_PROXY=external` и настройте HTTPS вручную. Подробные правила proxy и восстановления приведены во [встроенной инструкции](../../web/cabinet-installation.html).

[Брендинг и переводы](branding.md) · [Кабинет](sections/cabinet.md) · [Докупки](addons.md)
