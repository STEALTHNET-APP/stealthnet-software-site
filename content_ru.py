#!/usr/bin/env python3
"""Содержимое сайта. Разметка страниц лежит здесь, шаблон — в build.py.

Всё написано по тому, что панель действительно делает: команды взяты из
рабочих установщиков, ограничения — из проверок живым движком. Если
что-то в панели изменится, править нужно и здесь, иначе документация
начнёт врать, а это хуже её отсутствия.
"""


# Репозиторий с кодом ещё не открыт, поэтому ссылки в шапке и на кнопке
# ведут на организацию — иначе посетитель упирается в 404. Когда код
# выложат, достаточно поменять GH_LINK на GH_REPO здесь: и шапка, и
# подвал, и кнопка на витрине берут адрес отсюда.
GH_ORG = 'https://github.com/STEALTHNET-APP'
GH_REPO = 'https://github.com/STEALTHNET-APP/stealthnet-software'
GH_LINK = GH_ORG


def ic(path):
    return f'<span class="ic"><svg viewBox="0 0 24 24">{path}</svg></span>'


ICONS = {
    'server': '<rect x="2" y="3" width="20" height="7" rx="2"/><rect x="2" y="14" width="20" height="7" rx="2"/>',
    'shield': '<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10Z"/>',
    'card': '<path d="M20 7H4a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2Z"/><path d="M2 11h20"/>',
    'chat': '<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2Z"/>',
    'chart': '<path d="M3 17 9 11l4 4 8-8"/><path d="M15 7h6v6"/>',
    'link': '<path d="M10 13a5 5 0 0 0 7.5.5l3-3a5 5 0 0 0-7-7l-1.7 1.7"/><path d="M14 11a5 5 0 0 0-7.5-.5l-3 3a5 5 0 0 0 7 7l1.7-1.7"/>',
    'code': '<path d="m4 17 6-6-6-6"/><path d="M12 19h8"/>',
    'lock': '<rect x="4" y="10" width="16" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/>',
}


def build(landing, doc_page, latest):
    # ─────────────────────────── витрина ───────────────────────────
    landing(f'''
<div class="wrap hero">
  <span class="pill">открытый код · ставится на свой сервер</span>
  <h1>Панель для тех,<br>кто продаёт VPN</h1>
  <p class="lead">Ноды, подписки, тарифы, приём оплат и телеграм-бот — в одном месте.
     Ничего не отправляется наружу: панель, база и сервис подписок работают на вашем железе.</p>
  <div class="cta">
    <a class="btn primary" href="docs/index.html">
      <svg viewBox="0 0 24 24"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
      Быстрый старт</a>
    <a class="btn" href="{GH_LINK}">Исходный код</a>
  </div>
  <p class="note">Rust и PostgreSQL. Одна команда для установки, одна для подключения ноды.</p>
</div>

<section class="wrap" id="features">
  <h2>Что внутри</h2>
  <p class="sub">Не список галочек, а то, чем действительно приходится пользоваться каждый день.</p>
  <div class="grid">
    <div class="card"><h3>{ic(ICONS['server'])}Ноды</h3>
      <p>Агент держит связь с панелью, применяет профиль и отдаёт трафик, онлайн и нагрузку.
         Подключение — одной командой; гео-базы обновляются сами.</p></div>
    <div class="card"><h3>{ic(ICONS['shield'])}Протоколы</h3>
      <p>VLESS, VMess, Trojan, Shadowsocks 2022, Hysteria2. Транспорты: Reality, WebSocket,
         gRPC, XHTTP, HTTPUpgrade, mKCP. Одиннадцать готовых заготовок конфигурации.</p></div>
    <div class="card"><h3>{ic(ICONS['link'])}Подписки</h3>
      <p>Xray JSON, Clash, sing-box, base64 — формат выбирается по приложению клиента.
         Сервис подписок выносится на отдельный домен, чтобы блокировка панели не отрезала людей.</p></div>
    <div class="card"><h3>{ic(ICONS['card'])}Оплата</h3>
      <p>Тарифы с периодами, промокоды, партнёрская программа, автопродление.
         Платёжные модули подключаются ключами прямо в панели, без правки файлов.</p></div>
    <div class="card"><h3>{ic(ICONS['chat'])}Телеграм-бот</h3>
      <p>Продажа и продление в боте, тексты и кнопки редактируются в панели.
         Рассылки по сегментам с отложенной отправкой.</p></div>
    <div class="card"><h3>{ic(ICONS['chart'])}Учёт</h3>
      <p>Трафик по клиентам и нодам, лимиты и сброс по расписанию, устройства по HWID,
         расходы на серверы и прибыль после них.</p></div>
  </div>
</section>

<section class="wrap">
  <h2>Установка</h2>
  <p class="sub">Нужен сервер с Debian или Ubuntu, домен для панели и домен для подписок.</p>
  <pre><code>git clone https://github.com/STEALTHNET-APP/stealthnet-software.git
cd stealthnet-software
cp .env.example .env    # адреса, токен бота, доступ к базе
docker compose up -d</code></pre>
  <p style="color:var(--text-2)">Дальше — <a href="docs/index.html">быстрый старт</a>:
     первый администратор, профиль конфигурации, первая нода и первый клиент.</p>
</section>

<section class="wrap">
  <h2>Честно о границах</h2>
  <p class="sub">Чтобы вы не узнали об этом после установки.</p>
  <div class="grid">
    <div class="card"><h3>{ic(ICONS['lock'])}Одна валюта</h3>
      <p>Цены и отчёты ведутся в одной валюте на весь сервис. Несколько сразу означали бы
         курс, пересчёт и расхождения в отчётности. Исключение — Telegram Stars: у них
         своя единица и своя цена.</p></div>
    <div class="card"><h3>{ic(ICONS['code'])}Плагины нод</h3>
      <p>Каталога расширений нет. Блокировки и обходы делаются правилами маршрутизации
         в профиле конфигурации — этого хватает для почти всех задач.</p></div>
    <div class="card"><h3>{ic(ICONS['server'])}Смена состава клиентов</h3>
      <p>Движок читает список клиентов при запуске, поэтому покупка или отзыв подписки
         означает его перезапуск на ноде. Окно — до 15 секунд.</p></div>
  </div>
</section>

<section class="wrap">
  <h2>Что нового</h2>
  <p class="sub">Все изменения панели — на отдельной странице, свежие сверху.</p>
  <div class="release">
    <span class="pill">{latest['title']}</span>
    <p>{latest['note']}</p>
    <ul>{''.join(f'<li>{x}</li>' for x in latest['items'])}</ul>
    <a class="btn" href="docs/changelog.html#{latest['anchor']}">Весь список изменений</a>
  </div>
</section>
''')

    # ─────────────────────── быстрый старт ───────────────────────
    doc_page('index.html', 'Быстрый старт',
             'Установка панели STEALTHNET SOFTWARE, первая нода и первый клиент.', '''
<p class="lead-p">За один проход: поднять панель, создать профиль конфигурации,
подключить ноду, выдать клиенту подписку и убедиться, что она работает.</p>

<h2 id="requirements">Что понадобится</h2>
<ul>
  <li>Сервер с Debian 12 или Ubuntu 22.04+, root-доступ.</li>
  <li>Домен для панели и отдельный домен для подписок — их A-записи должны указывать на сервер.</li>
  <li>Токен телеграм-бота, если продажи идут через бота.</li>
</ul>
<div class="note"><b>Почему подписки на отдельном домене.</b> Если домен панели заблокируют,
клиенты продолжат обновлять конфиги: сервис подписок работает независимо и умеет жить
на другом сервере, имея только ключ доступа к API.</div>

<h2 id="install">Установка</h2>
<pre><code>git clone https://github.com/STEALTHNET-APP/stealthnet-software.git
cd stealthnet-software
cp .env.example .env</code></pre>
<p>В <code>.env</code> заполните адреса панели и подписок, доступ к базе и токен бота.
Затем:</p>
<pre><code>docker compose up -d
docker compose logs -f api</code></pre>
<p>В логе появится строка с логином и одноразовым паролем первого администратора.
Смените пароль сразу после входа.</p>

<h2 id="profile">Профиль конфигурации</h2>
<p>Профиль — это конфиг Xray, который раздаётся нодам. Один профиль обслуживает
много нод: клиентов в него вписывает агент на месте.</p>
<ol>
  <li>Откройте <b>Инфраструктура → Профили конфигураций → Новый профиль</b>.</li>
  <li>Выберите заготовку. Для начала — <b>VLESS + Reality (TCP)</b>: она не требует
      ни своего домена, ни сертификата.</li>
  <li>Откройте редактор и замените поля, написанные заглавными буквами: ключи берутся
      кнопкой «Ключи Reality» на той же странице.</li>
  <li>Сохраните. Панель проверит конфиг настоящим движком и не даст сохранить нерабочий.</li>
</ol>
<div class="note warn"><b>Плейсхолдеры вроде <code>ВАШ_ПРИВАТНЫЙ_КЛЮЧ</code> не пройдут проверку
намеренно.</b> Так о забытом поле становится известно сразу, а не от клиентов через неделю.</div>

<h2 id="node">Первая нода</h2>
<p>На странице <b>Ноды</b> нажмите «Подключить ноду». Панель выдаст готовую команду —
выполните её на сервере ноды. Подробности и особые случаи: <a href="nodes.html">Ноды</a>.</p>
<p>Через 15 секунд нода появится в списке зелёной. Если она красная — панель покажет
причину прямо в строке.</p>

<h2 id="squad">Сквад и тариф</h2>
<p>Сквад — это набор инбаундов, который выдаётся клиенту. Цепочка такая:</p>
<pre><code>тариф → сквады → инбаунды → ноды</code></pre>
<ol>
  <li><b>Внутренние сквады → Создать сквад</b>, отметьте инбаунды профиля.</li>
  <li><b>Тарифы → Новый тариф</b>: код, название, срок и цена, лимит трафика и устройств,
      и сквады, которые получит клиент.</li>
</ol>

<h2 id="host">Хост</h2>
<p>Хост — это строка, которую клиент увидит локацией в приложении. Он ссылается на
инбаунд профиля и добавляет то, что знает только администратор: внешний адрес, порт,
имя локации.</p>
<p><b>Хосты → Добавить хост</b>: ремарка (например «🇩🇪 Франкфурт»), адрес и порт ноды,
инбаунд. Порядок хостов в списке — это порядок локаций у клиента.</p>

<h2 id="client">Первый клиент</h2>
<p><b>Клиенты → Создать клиента</b>. Укажите имя и тариф. В карточке, на вкладке
«Подписка и VPN», будет ссылка — её и отдают клиенту.</p>
<p>Проверить, что всё сошлось, можно не выходя из панели: откройте ссылку в браузере —
появится страница с приложениями под каждую платформу и кнопкой «Добавить подписку».</p>

<div class="note"><b>Ничего не отдаётся?</b> Подписка не выдаёт конфиги, если у клиента
нет активного срока, или ни один инбаунд его сквадов не поднят на живой ноде.
Обе причины видны в <a href="troubleshooting.html">разборе поломок</a>.</div>
''', nxt=('nodes.html', 'Ноды'))

    # ─────────────────────────── ноды ───────────────────────────
    doc_page('nodes.html', 'Ноды',
             'Подключение ноды, сертификаты, гео-базы и что означают её состояния.', '''
<p class="lead-p">Нода — это сервер с движком Xray и нашим агентом. Агент забирает
профиль из панели, вписывает в него клиентов и отчитывается трафиком и нагрузкой.</p>

<h2 id="install">Подключение</h2>
<p>На странице <b>Ноды</b> нажмите «Подключить ноду» — панель соберёт команду с адресом
и ключом. Она ставит движок, агент и systemd-службу:</p>
<pre><code>PANEL_URL=https://панель.ваш-домен NODE_SECRET=... bash install-node.sh</code></pre>
<p>Секрет показывается один раз: в базе хранится только его хэш. Потеряли — перевыпустите
в меню ноды.</p>

<h2 id="tls">Сертификат</h2>
<p>Reality сертификат не нужна — она прикрывается чужим сайтом. А Trojan, Hysteria2
и всё, что работает поверх TLS, без сертификата не поднимутся вовсе.</p>
<p>На чистой ноде сертификата взяться неоткуда, поэтому установщик выпускает его сам,
если задать домен:</p>
<pre><code>NODE_DOMAIN=de1.ваш-домен PANEL_URL=... NODE_SECRET=... bash install-node.sh</code></pre>
<p>Домен должен указывать A-записью на эту машину, а порт 80 быть свободен: проверка
Let's Encrypt ходит именно туда. Сертификат кладётся в <code>/etc/sn-node/tls/</code>,
продление проверяется ежедневно, и при обновлении движок перезапускается — иначе через
три месяца он продолжал бы отдавать просроченный.</p>
<div class="note"><b>В профиле</b> укажите пути к этим файлам в
<code>streamSettings.tlsSettings.certificates</code>.</div>

<h2 id="geo">Гео-базы</h2>
<p>Правила вида <code>geoip:ru</code> и <code>geosite:category-ads-all</code> опираются
на файлы <code>geoip.dat</code> и <code>geosite.dat</code>. Из архива движка они ставятся
один раз и дальше устаревают, поэтому установщик заводит еженедельное обновление.</p>
<p>Заодно ставится <code>zapret.dat</code> — список заблокированных в России ресурсов.
Он нужен, только если в правилах есть <code>zapret:*</code>; без файла движок с таким
правилом не запустится.</p>
<p>Обновление подменяет файл, лишь когда он действительно изменился: движок читает базы
при старте, а лишний перезапуск — это лишний обрыв всех соединений.</p>

<h2 id="states">Состояния ноды</h2>
<table>
  <tr><th>Вид</th><th>Что означает</th></tr>
  <tr><td>Зелёная</td><td>Агент на связи, движок работает, клиенты обслуживаются.</td></tr>
  <tr><td>Красная</td><td>Либо агент не выходит на связь, либо <b>движок не запустился</b> —
      причина написана прямо в строке. Второе бывает при ошибке в конфиге или пропавшем сертификате.</td></tr>
  <tr><td>Серая</td><td>Выключена администратором. Это не авария: она намеренно не обслуживает клиентов.</td></tr>
</table>
<div class="note"><b>Агент на связи ≠ движок работает.</b> Это разные состояния, и панель
их различает: нода, отвечающая на опрос при лежащем Xray, показывается аварийной,
а не «в строю».</div>

<h2 id="safety">Защита от битого конфига</h2>
<p>Перед подменой рабочего конфига агент спрашивает движок, примет ли он новый. Если нет —
остаётся на прежнем и сообщает причину в панель. Одна опечатка в новом протоколе не
выключает ноду целиком вместе с работавшими локациями.</p>

<h2 id="traffic">Трафик и онлайн</h2>
<p>Счётчики движок ведёт по клиентам и инбаундам, агент забирает их раз в 15 секунд
и присылает дельту. «В сети» означает «передавал трафик за последнее окно опроса»:
перечень активных соединений Xray наружу не отдаёт, и это честное приближение.</p>
''', prev=('index.html', 'Быстрый старт'), nxt=('protocols.html', 'Протоколы'))

    # ───────────────────── протоколы ─────────────────────
    doc_page('protocols.html', 'Протоколы и профили',
             'Какие протоколы и транспорты работают, готовые заготовки конфигурации.', '''
<p class="lead-p">Профиль — это конфиг Xray, который раздаётся нодам. В панели есть
одиннадцать готовых заготовок: выбрали, создалось, дальше правите под себя.</p>

<h2 id="works">Что работает</h2>
<p>Проверено живым трафиком, а не по документации: каждый вариант поднимался на сервере,
и через него запрашивалась внешняя страница.</p>
<table>
  <tr><th>Протоколы</th><td>VLESS, VMess, Trojan, Shadowsocks 2022, Hysteria2</td></tr>
  <tr><th>Транспорты</th><td>TCP (raw), WebSocket, gRPC, XHTTP, HTTPUpgrade, mKCP</td></tr>
  <tr><th>Шифрование</th><td>Reality, TLS, без шифрования (за обратным прокси)</td></tr>
</table>

<h2 id="limits">Чего движок не умеет</h2>
<p>Это ограничения Xray, а не панели:</p>
<ul>
  <li><b>h2 и QUIC</b> удалены из Xray 26 — движок прямо предлагает перейти на XHTTP.</li>
  <li><b>mKCP</b> есть, но поля <code>header</code> и <code>seed</code> из него убрали;
      старый синтаксис отвергается.</li>
  <li><b>TUIC</b> отсутствует.</li>
  <li><b>WireGuard</b> движок принимает, но это не подписочный протокол: там пары ключей
      на каждого участника, а не выдача клиентам.</li>
</ul>
<p>Панель отклоняет такие конфиги с причиной от самого движка, а не общим «ошибка».</p>

<h2 id="presets">Заготовки</h2>
<table>
  <tr><th>Заготовка</th><th>Когда брать</th></tr>
  <tr><td>VLESS + Reality (TCP)</td><td>Основной вариант. Не нужен ни домен, ни сертификат.</td></tr>
  <tr><td>VLESS + Reality (gRPC)</td><td>Иногда проходит там, где режут обычный TCP.</td></tr>
  <tr><td>VLESS + WebSocket</td><td>За обратным прокси или CDN.</td></tr>
  <tr><td>VLESS + XHTTP</td><td>Живуч там, где веб-сокеты обрывают.</td></tr>
  <tr><td>VLESS + HTTPUpgrade</td><td>Легче веб-сокета, проходит через большинство прокси.</td></tr>
  <tr><td>VLESS + mKCP</td><td>Поверх UDP, лучше держится на плохой линии.</td></tr>
  <tr><td>VMess + WebSocket</td><td>Если у клиентов приложения без поддержки VLESS.</td></tr>
  <tr><td>Trojan + TLS</td><td>Выглядит обычным HTTPS-сайтом. Нужен домен и сертификат.</td></tr>
  <tr><td>Shadowsocks 2022</td><td>Без TLS вообще, отдельные ключи у каждого клиента.</td></tr>
  <tr><td>Hysteria2</td><td>Своё управление перегрузкой, хорошо держит потери пакетов.</td></tr>
  <tr><td>Reality + WebSocket</td><td>Две точки входа в одном профиле: основная и запасная.</td></tr>
</table>

<h2 id="hysteria">Hysteria2: две тонкости</h2>
<div class="note warn">
  <p><b>В Xray протокол называется <code>hysteria</code>, а не <code>hysteria2</code>.</b>
  Со вторым написанием движок отвечает <code>unknown config id</code>. Версия указывается
  отдельно: <code>"version": 2</code>.</p>
  <p style="margin:8px 0 0"><b>Реализация идёт поверх обычного транспорта, а не поверх QUIC.</b>
  Поэтому подключаются клиенты на движке Xray, а отдельный QUIC-сервер hysteria к ней
  не подходит.</p>
</div>
<p>Сертификат обязателен — см. <a href="nodes.html#tls">сертификат на ноде</a>.</p>

<h2 id="shadowsocks">Shadowsocks: только 2022</h2>
<p>У классических методов один пароль на всех, и раздать разным клиентам разные ключи
невозможно. Поэтому годятся только методы <code>2022-*</code>.</p>
<p>В настройках инбаунда обязателен <code>password</code> — это серверный ключ. Клиент
подключается парой «серверный ключ : личный ключ», и без серверной половины движок
отвечает <code>missing psk</code>. Личный ключ панель выводит из UUID клиента, поэтому
отзыв подписки отключает доступ и здесь.</p>

<h2 id="routing">Маршрутизация</h2>
<p>Блокировки и обходы делаются правилами в <code>routing.rules</code>. В редакторе есть
готовые блоки: блокировка торрентов и рекламы, российские сайты напрямую, заблокированные
в РФ в туннель, цепочка на вторую ноду, свой DNS.</p>
<div class="note"><b>Приватные сети перечисляйте списком</b>, а не через
<code>geoip:private</code>: если файла на ноде не окажется, правило со ссылкой на него
уронит весь конфиг.</div>

<h2 id="service">Служебный инбаунд</h2>
<p><code>api-in</code> слушает только петлю и существует ради статистики — через него агент
читает счётчики. Без него не работают лимиты трафика. В списках выбора локаций он не
показывается: подключиться к нему нельзя.</p>
''', prev=('nodes.html', 'Ноды'), nxt=('subscriptions.html', 'Подписки'))

    # ───────────────────── подписки ─────────────────────
    doc_page('subscriptions.html', 'Подписки',
             'Форматы выдачи, отдельный сервер подписок, устройства и лимиты.', '''
<p class="lead-p">Подписка — единственное, что видит клиент. По одной ссылке приложение
получает список локаций в том формате, который понимает.</p>

<h2 id="formats">Форматы</h2>
<p>Формат определяется по User-Agent приложения. Правила проверяются сверху вниз,
срабатывает первое подходящее; если не подошло ни одно — решает встроенный список
известных приложений.</p>
<table>
  <tr><th>Формат</th><th>Кому</th></tr>
  <tr><td>Xray JSON</td><td>Happ, Streisand, v2Box, NekoBox</td></tr>
  <tr><td>Clash / Mihomo</td><td>Clash Meta, FlClash, Stash</td></tr>
  <tr><td>sing-box</td><td>sing-box, Hiddify</td></tr>
  <tr><td>base64-список</td><td>v2rayNG, Shadowrocket и всё неизвестное</td></tr>
  <tr><td>Веб-страница</td><td>Браузер: приложения под платформу и кнопка установки</td></tr>
</table>
<p>Проверить, что именно получит конкретное приложение, можно в панели: раздел
<b>Правила ответов</b>, поле «Вставьте User-Agent».</p>

<h2 id="separate">Отдельный сервер</h2>
<p>Сервис подписок выносится на другой сервер и другой домен: тогда блокировка панели
не отрезает клиентов от конфигов, а машина с подписками вообще не имеет доступа к базе.</p>
<pre><code>SUB_MODE=api
PANEL_URL=https://панель.ваш-домен
SUB_SERVICE_TOKEN=&lt;ключ из панели&gt;</code></pre>
<p>Ключ выпускается в <b>Настройках → Сервис подписки</b> и показывается один раз.
Там же панель отдаёт готовые команды установки и блок для Caddy.</p>
<div class="note"><b>Рядом с панелью</b> сабка читает базу напрямую: это быстрее похода
в API, и служебный ключ ей вообще не нужен.</div>

<h2 id="devices">Устройства и лимит</h2>
<p>Приложения представляются заголовками (<code>x-hwid</code>, <code>x-device-os</code>,
<code>x-device-model</code>, <code>x-app-version</code>). По ним панель ведёт список
устройств клиента и следит за лимитом из тарифа.</p>
<p>Лимит проверяется только для нового устройства: уже зарегистрированное продолжает
работать, даже если лимит потом уменьшили — иначе человек внезапно терял бы доступ на
телефоне, которым пользовался вчера.</p>
<p>При превышении клиент получает не пустоту, а понятную строку в списке локаций:
сколько устройств занято и что делать.</p>
<div class="note">Браузер и <code>curl</code> устройствами не считаются: они этих
заголовков не шлют.</div>

<h2 id="inactive">Когда подписка не отдаёт конфиги</h2>
<ul>
  <li>Срок истёк или закончился трафик.</li>
  <li>Клиент отключён администратором.</li>
  <li>Ни один инбаунд его сквадов не поднят на живой ноде.</li>
  <li>Исчерпан лимит устройств.</li>
</ul>
<p>Во всех случаях вместо локаций приходит причина — её текст настраивается в
<b>Настройках подписки</b> и поддерживает подстановки <code>{date}</code>,
<code>{days}</code>, <code>{tariff}</code>.</p>

<h2 id="history">Кто запрашивал</h2>
<p>Раздел <b>Запросы подписки</b> показывает, кто, чем и когда обращался по ссылке.
Три запроса с одного адреса — норма, это обновление по интервалу. Десятки разных
адресов за час означают, что ссылка утекла: отзовите подписку из карточки клиента,
это сменит короткий идентификатор и убьёт старую ссылку.</p>
''', prev=('protocols.html', 'Протоколы'), nxt=('billing.html', 'Тарифы и оплата'))

    # ───────────────────── тарифы ─────────────────────
    doc_page('billing.html', 'Тарифы и оплата',
             'Тарифы, периоды, промокоды, платёжные модули и валюта.', '''
<p class="lead-p">Тариф связывает цену, лимиты и доступы: сколько стоит, сколько трафика
и устройств, какие локации получит клиент.</p>

<h2 id="currency">Валюта одна</h2>
<div class="note warn"><b>На весь сервис действует одна валюта.</b> Несколько сразу
означали бы курс, пересчёт и расхождения в отчётности. Меняется в
<b>Платёжки и валюты</b>, но уже проставленные цены не пересчитываются — их нужно
перепроверить вручную.</div>
<p>Исключение — Telegram Stars: они живут в собственных единицах и обычные деньги принять
не могут, поэтому цена в звёздах задаётся отдельным полем.</p>

<h2 id="tariffs">Тарифы</h2>
<ul>
  <li><b>Код</b> — служебный, по нему находятся уже сделанные покупки. У работающего
      тарифа менять не стоит.</li>
  <li><b>Периоды и цены</b> — сколько дней и почём. Периодов может быть несколько.</li>
  <li><b>Лимиты</b> — трафик, устройства, стратегия сброса счётчика.</li>
  <li><b>Сквады</b> — набор локаций, который получит клиент.</li>
  <li><b>Видимость</b> — скрытый тариф доступен только по прямой ссылке или промокоду.</li>
</ul>
<p>Порядок тарифов в списке — это порядок в боте и на сайте, строки перетаскиваются.</p>

<h2 id="providers">Платёжные модули</h2>
<p>Ключи вносятся в панели, а не в файлах на сервере. Они хранятся в базе и обратно
в браузер не отдаются: показывается только признак «задан». Пустое поле при сохранении
означает «оставить прежний ключ».</p>
<div class="note"><b>Цена бесполезна, если её валюту не принимает ни один включённый
модуль:</b> клиент увидит сумму, но не увидит кнопку оплаты. Панель предупреждает об этом
на странице тарифа.</div>
<p>Для провайдера с обычным REST API есть универсальный модуль «Своя платёжка»: принимает
POST с суммой, возвращает ссылку на оплату и ждёт вебхук со статусом. Кода писать не нужно.</p>

<h2 id="refund">Возврат</h2>
<p>Деньги возвращаются в личном кабинете провайдера — панель этого не делает за вас:
у разных провайдеров возврат устроен по-разному, и тихая попытка «вернуть автоматически»
при неудаче пометила бы платёж возвращённым при живых деньгах.</p>
<p>В панели фиксируется факт возврата и, по желанию, отзывается выданная подписка.
Частичный возврат оставляет платёж успешным и записывает вернувшуюся сумму; полный
переводит его в «возвращён», чтобы выручка за месяц не включала эти деньги.</p>

<h2 id="promos">Промокоды и партнёры</h2>
<p>Промокоды дают процент скидки, фиксированную сумму или бонусные дни; можно ограничить
числом применений, сроком и «только первая покупка».</p>
<p>Партнёр получает процент с каждой успешной оплаты приведённого клиента, пожизненно.
Новая ставка применяется к будущим платежам — уже начисленные комиссии не пересчитываются.</p>

<h2 id="infra">Расходы на серверы</h2>
<p>У каждой ноды указываются хостер, стоимость аренды и день списания. Раздел
<b>Инфра-биллинг</b> сводит это вместе: во что обходится парк и сколько остаётся
после расходов.</p>
''', prev=('subscriptions.html', 'Подписки'), nxt=('troubleshooting.html', 'Если что-то не работает'))

    # ───────────────────── разбор поломок ─────────────────────
    doc_page('troubleshooting.html', 'Если что-то не работает',
             'Разбор частых поломок: нода красная, клиент не подключается, подписка пуста.', '''
<p class="lead-p">Порядок один: сначала смотрим, работает ли движок на ноде, потом —
доходит ли до клиента конфиг, и только потом грешим на приложение.</p>

<h2 id="node-red">Нода горит красной</h2>
<p>Наведите на значок — в подсказке будет причина. Два разных случая:</p>
<h3>Агент не выходит на связь</h3>
<pre><code>systemctl status sn-node
journalctl -u sn-node -n 50 --no-pager</code></pre>
<p>Чаще всего это неверный адрес панели или протухший секрет ноды. Секрет
перевыпускается в меню ноды, после чего команду установки нужно выполнить заново.</p>
<h3>Движок не запустился</h3>
<p>Причина написана в строке ноды. Проверить руками:</p>
<pre><code>xray -test -config /etc/sn-node/config.json</code></pre>
<p>Типичные причины: незаполненные поля из заготовки, отсутствующий файл сертификата,
занятый порт, правило со ссылкой на несуществующую гео-базу.</p>
<div class="note"><b>Ноду это не выключает.</b> Агент не подменяет рабочий конфиг тем,
который движок отверг, — прежние локации продолжают обслуживаться.</div>

<h2 id="empty-sub">Подписка отдаёт пусто</h2>
<p>Проверьте по цепочке:</p>
<ol>
  <li>У клиента активный срок и остаток трафика.</li>
  <li>Клиент состоит в скваде.</li>
  <li>В скваде отмечены инбаунды.</li>
  <li>Эти инбаунды привязаны к ноде, и нода в сети.</li>
  <li>У инбаундов есть хосты, и хосты включены.</li>
</ol>
<p>Достаточно одного разрыва — и локаций у клиента не будет.</p>

<h2 id="new-client">Новый клиент не подключается</h2>
<p>Состав клиентов доезжает до ноды отдельно от конфига, в течение 15 секунд.
Посмотрите в журнале агента:</p>
<pre><code>journalctl -u sn-node --since '2 min ago' | grep применяем</code></pre>
<p>Строка вида <code>применяем ... причина="изменился состав клиентов"</code> означает,
что изменение доехало.</p>

<h2 id="cert">Перестал работать Trojan или Hysteria2</h2>
<p>Почти всегда это истёкший сертификат. Проверьте:</p>
<pre><code>ls -l /etc/sn-node/tls/
openssl x509 -in /etc/sn-node/tls/ваш-домен.crt -noout -enddate</code></pre>
<p>Обновление и перезапуск движка делает ежедневная служба <code>sn-cert-sync</code>.
Если её нет, переустановите ноду с переменной <code>NODE_DOMAIN</code>.</p>

<h2 id="no-traffic">Не считается трафик</h2>
<p>Проверьте, что в профиле есть служебный инбаунд <code>api-in</code> и секции
<code>api</code>, <code>stats</code>, <code>policy</code>. Без них движок не отдаёт
счётчики, а значит не работают и лимиты в тарифах. Во всех заготовках они уже есть.</p>

<h2 id="logs">Куда смотреть</h2>
<table>
  <tr><th>Служба</th><th>Команда</th></tr>
  <tr><td>Панель</td><td><code>journalctl -u sn-api -n 100</code></td></tr>
  <tr><td>Подписки</td><td><code>journalctl -u sn-sub -n 100</code></td></tr>
  <tr><td>Агент ноды</td><td><code>journalctl -u sn-node -n 100</code></td></tr>
  <tr><td>Бот</td><td><code>journalctl -u sn-bot -n 100</code></td></tr>
</table>
<p>Состояние служб целиком — в панели, в <b>Справке</b>: там проверяются API, база
и сервис подписок.</p>
''', prev=('billing.html', 'Тарифы и оплата'), nxt=('faq.html', 'Вопросы'))

    # ───────────────────────── вопросы ─────────────────────────
    doc_page('faq.html', 'Вопросы',
             'Частые вопросы о панели STEALTHNET SOFTWARE.', f'''
<h2 id="remnawave">Чем отличается от других панелей</h2>
<p>Написана с нуля на Rust, ставится одной командой, всё в одном репозитории — панель,
сервис подписок, бот, воркер и агент ноды. Разделены артефакты, а не исходники:
каждая служба — свой бинарник и свой образ, и сервис подписок разворачивается
на отдельном сервере.</p>

<h2 id="data">Что уходит наружу</h2>
<p>Ничего. Панель, база и сервис подписок работают на вашем железе. Значки хостеров
рисуются на месте, а не подгружаются со сторонних сервисов — иначе такой запрос
рассказал бы им, какими серверами вы пользуетесь.</p>

<h2 id="domains">Сбор посещаемых сайтов</h2>
<p>Выключен по умолчанию и включается осознанно. Это журнал сайтов, куда ходят ваши
клиенты — самое ценное, что может утечь из VPN-панели. Прежде чем включать, убедитесь,
что это допустимо по вашей политике и по закону страны, где вы работаете.</p>

<h2 id="restart">Почему покупка перезапускает движок</h2>
<p>Xray читает список клиентов при запуске: добавлять их на ходу его API в текущей версии
не умеет — есть только управление инбаундами. Поэтому изменение состава означает
перезапуск. Окно опроса — 15 секунд, соединения на этот момент рвутся.</p>

<h2 id="languages">Языки</h2>
<p>Русский и английский, переключатель в шапке. Перевод накладывается после отрисовки,
поэтому добавление строки не требует правки каждого экрана.</p>

<h2 id="backup">Резервные копии</h2>
<p>Достаточно дампа PostgreSQL: в нём и клиенты, и тарифы, и профили конфигураций.
Файлы на нодах восстанавливаются повторным запуском установщика.</p>

<h2 id="contribute">Где взять код</h2>
<p>Репозиторий с исходниками ещё закрыт: он откроется вместе с первым релизом,
в организации <a href="{GH_LINK}">STEALTHNET-APP</a>. Команды установки на этих
страницах написаны под него, так что после публикации они заработают как есть.</p>
<p>Пока открыт только <a href="{GH_ORG}/stealthnet-software-site">репозиторий сайта</a> —
опечатку или неточность в документации можно поправить прямо там.</p>
''', prev=('troubleshooting.html', 'Если что-то не работает'))
